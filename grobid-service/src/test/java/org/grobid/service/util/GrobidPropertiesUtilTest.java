package org.grobid.service.util;

import java.io.File;
import java.io.IOException;

import org.junit.Test;
import static org.junit.Assert.*;

public class GrobidPropertiesUtilTest {
/*	
	
	@Test
	public void testgetTypeBooleanUnknown(){
		assertEquals("Returned type not the one expected", GrobidProperty.TYPE.UNKNOWN, GrobidPropertiesUtil.getType(""));
	}
	
	@Test
	public void testgetTypeBooleanTrue(){
		assertEquals("Returned type not the one expected", GrobidProperty.TYPE.BOOLEAN, GrobidPropertiesUtil.getType(" TruE "));
	}
	@Test
	public void testgetTypeBooleanFalse(){
		assertEquals("Returned type not the one expected", GrobidProperty.TYPE.BOOLEAN, GrobidPropertiesUtil.getType(" False "));
	}

	@Test
	public void testgetTypeInteger(){
		assertEquals("Returned type not the one expected", GrobidProperty.TYPE.INTEGER, GrobidPropertiesUtil.getType(" 88 "));
	}
	
	@Test
	public void testgetTypeFile() throws IOException{
		File file = File.createTempFile("file", "test");
		assertEquals("Returned type not the one expected", GrobidProperty.TYPE.FILE, GrobidPropertiesUtil.getType(file.getCanonicalPath()));
	}
	
	@Test
	public void testgetTypeString() throws IOException{
		assertEquals("Returned type not the one expected", GrobidProperty.TYPE.STRING, GrobidPropertiesUtil.getType(" string test "));
	}
	*/
}
